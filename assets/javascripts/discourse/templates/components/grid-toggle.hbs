{{#if this.parentView.category.user_grid_view}}
    {{d-button
          class="btn btn-default btn-icon-text grid-view-toggle"
          action="gridToggle"
          title="Grid is on"
          translatedLabel="Grid Off"}}
{{else}}
    {{d-button
          class="btn btn-default btn-icon-text grid-view-toggle"
          action="gridToggle"
          title="Grid is off"
          translatedLabel="Grid On"}}

{{/if}}
